{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Run pytest with hatch",
            "command": "hatch run pytest",
            "group": "test"
        }
    ]
}
